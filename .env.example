# Configuración para Hostinger / Producción
# Renombra este archivo a .env antes de compilar (npm run build)

# URL base de la API (ajusta según tu dominio)
# Ejemplo: https://unionjeguera.com/api/api.php
VITE_API_URL=/api/api.php

# URL base para subida de archivos
VITE_UPLOAD_URL=/api/upload.php

# Modo de la aplicación
NODE_ENV=production
